# Java 知识点随笔

# 异步回调
```
在正常的业务中使用同步线程，如果服务器每处理一个请求，就创建一个线程的话，会对服务器的资源造成浪费。
因为这些线程可能会浪费时间在等待网络传输，等待数据库连接等其他事情上，真正处理业务逻辑的时间很短。
但是其他线程在线程池满了之后又会阻塞，等待前面的线程处理完成。
而且会出现一个奇怪的现象，客户端的请求被阻塞，但是 cpu 资源使用却很低。这就导致并发量并不高

而异步可以解决这个问题

那就是使用回调来实现异步
```